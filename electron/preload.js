// Preload script — runs in an isolated context before the renderer loads.
// Currently exposes nothing. Reserved for future use, e.g.:
//   const { contextBridge } = require('electron');
//   contextBridge.exposeInMainWorld('appInfo', { version: '0.1.0' });
